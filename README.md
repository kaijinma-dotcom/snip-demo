# bundle

> **This branch is generated output — do not hand-edit.**
>
> It is assembled automatically by `scripts/build-bundle.mjs` on the `main` branch.
> Run `node scripts/build-bundle.mjs --push` from the superproject root to rebuild.
