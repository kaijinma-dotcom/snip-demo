{
  "name": "snip-bundle",
  "version": "1.0.0",
  "description": "Snip — self-contained backend + SPA bundle",
  "main": "server.js",
  "scripts": {
    "start": "bun server.js"
  },
  "engines": {
    "bun": ">=1"
  }
}
